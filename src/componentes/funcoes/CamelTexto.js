const formatoCamel = (textoCamel) => {
    const formata = textoCamel.toLowerCase().split(' ');
    return (
        //formata[0] = formata.slice(1).charAt(0)
        console.log(textoCamel)
    );
}
export {formatoCamel}